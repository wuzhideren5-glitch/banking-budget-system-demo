# System PDD Historical Merge Records - 2026-06-03

本文从 `docs/product/Banking_Budget_System_PDD.md` 当前正文中移出，只用于追溯历史合并、甄别和验收背景。

这些记录不是当前 schema、导航、接口或 Module 职责的权威入口；当前开发必须回到 `CONTEXT.md`、`docs/development/current-system-map.md`、`docs/product/Banking_Budget_Files.md`、Database PDD 和 System PDD 当前正文核对。

## 历史合并记录（仅追溯，不作为当前口径入口）

以下按日期保留历史合并、甄别和修正记录，用于追溯来源、验收背景和被拒绝的旧实现。它们不是当前 schema、导航、接口或 Module 职责的权威入口；新开发必须回到本文件当前权威正文、`current-system-map.md`、`CONTEXT.md` 和 Database PDD 核对。

## 0.21 本轮需求变更（2026-05-19）— 潘潘部门费用模块完善包

- 本轮从 `archive/team_packages/incoming/20260519/潘潘_20260519_部门费用模块完善规范打包/` 中增量合入部门费用预算管理功能，保持当前主线的数据科目、部门科目和上传入库逻辑为权威。
- **费用预测表**升级为增强版：支持主体、事业群、费用归属部门、预算科目等口径切换；支持事业群拆分查看、预算科目维度查看费用归属部门明细、导入预览、按事业群导出、单位切换和经营分析列。
- **费用预测规则配置路由**由 `apps/api/app/routers/expense_forecast_rules.py` 注册，规则读取、保存、版本复制、重算和模拟测算使用同一套当前规则口径；主路由只提供预测表上下文与写入适配，不再保留重复规则路由实现。
- **费用预算执行报表**补齐“本月环比增减额 / 本月环比%”指标，部门模式、科目模式、多工作表月报和查询导出均应跟随页面当前金额单位输出。
- 本轮不修改数据库结构、不修改主键、不修改数据科目/部门科目钩稽关系；如后续需要新增表字段或调整关系，必须先输出数据库 Excel 进行确认。

## 0.22 本轮数据初始化（2026-05-19）— 费用预测默认规则

- 费用预测规则、预测录入、测算结果表结构已在前序合并中具备，本轮补齐默认初始化数据，避免页面因缺少规则和结果行而只能展示空状态。
- 默认规则遵循后端原有费用预测逻辑配置模板：规则类型为“余额分摊”，参数包括逐月递增、等差金额、实际优先、尾月调差和不允许负数。
- 规则覆盖策略：有归口管理部门的叶子预算科目按归口部门建规则；没有归口管理部门的通用费用科目，仅按费用执行明细中实际出现过的“费用归属部门 + 预算科目”组合建规则，避免全部门全科目笛卡尔积。
- 本轮初始化生成 `2026 / 260519v1` 默认版本，后续用户可在“费用预测逻辑配置”中复制版本、调整规则参数，并在“费用预测表”中录入业务报送和资划建议后重算。

## 0.23 本轮数据修正（2026-05-19）— 部门科目维护表

- 部门科目维护表已按潘潘部门架构维护模版重建，页面口径固定为 `主体 -> 事业群 -> 费用归属部门`，不再展示旧知识库演示口径“开心账户 / 开鑫贷”。
- `dept_account` 只维护事业群和费用归属部门两级主数据；费用发生部门、预算部门明细和框架快照继续由费用整体框架相关表维护，不进入部门科目树。
- 本轮执行前已按用户要求输出主键影响确认 Excel；正式执行后保留 37 个部门科目主键，删除 40 个不在确认模版中的旧主键。后续如需新增主键、调整主键或改变钩稽关系，仍必须先输出数据库 Excel 并确认。
- 部门科目维护、费用预测表、费用执行报表读取的费用归属部门应以本轮 `dept_account` 和费用整体框架映射后的口径为准；不得再从前端 mock、知识库示例或历史种子中补默认部门。

## 0.24 本轮需求修正（2026-05-19 / 2026-06-02）— BI映射维护

- BI映射维护当前只保留两套可见维护页签：**BI-AI科目映射表** 与 **BI部门维护**；旧 **BI科目维护** 已从前端和公开维护 API 删除。
- **BI部门维护** 用于维护“归口管理部门 -> 费用归属部门”的导入补匹配规则。页面按事业群和费用归属部门分组展示，新增/编辑时先选择费用归属部门，再选择归口管理部门；费用归属部门可选择“其他”并由用户手动填写。
- **BI-AI科目映射表** 用于展示和重建 BI 六级科目到费用大类、费用类别与预算发布口径的映射，是当前唯一可见的 BI 科目类维护表。
- `manage_dept_owner_mapping` 已按潘潘来源库导入并经业务纠偏后保留 29 条预置映射；其中 `个金管理部 -> 消费者金融业务` 保留，`个人金融事业群` 是 `消费者金融业务` 的上级口径，不作为 BI部门维护映射到 `消费者金融业务`。
- 本轮不再保留旧 BI 科目私有表；`control_item_subject_mapping` 已从当前 schema、运行库和费用执行明细导入 fallback 中删除，只保留在退休表删除清单和历史审计中。当前费用执行明细的科目补匹配只读取 `bi_ai_subject_mapping`，部门补匹配只读取 `manage_dept_owner_mapping`。

## 0.25 本轮需求变更（2026-06-01）— 成本收入比实际导入与投入产出专题概览

- 本轮从 `archive/team_packages/incoming/20260601/TeamSubmit_20260601_panpan_expense_budget/` 中甄别合入成本收入比实际导入与投入产出专题概览功能。
- **业务支出成本收入比实际导入**挂载在“部门费用预算管理模块”下，支持按产品和月份下载实际数/预算数/预测数三表模板、上传预览并确认写入当前年度库。
- **业务支出成本收入比维护**挂载在“部门费用预算管理模块”下，用于维护投入/产出细项树与指标分子分母关系；不再挂在“系统配置中心”。
- **投入产出专题概览**挂载在“部门费用预算管理模块”下，支持主体、费用月份、产品群、产品范围、金额单位、全行总表/分产品明细切换和 Excel 导出；不再挂在“多维分析工具”目录下。
- 本轮不采用来源包中的旧 `BCIR_STANDARD` 正式模型，不新增 `bcir_report_definition`、`bcir_fact_value` 等第二套成本收入比数据库；页面和接口统一读取当前 `business_cost_income_*` 三表。
- 成本收入比维护表为空时，系统幂等初始化业务投入细项、业务产出细项和默认评估指标；已有配置时不覆盖人工维护内容。

## 0.26 本轮需求变更（2026-06-02）— TeamSubmit_20260529 费用执行明细与 BI-AI 映射外科式合并

- 本轮从归档团队包 `archive/team_packages/incoming/20260602/TeamSubmit_20260529_shier_expense_budget_root_unpacked/` 中仅甄别合入部门费用侧功能增量，不整包覆盖当前主线。
- **BI映射维护**保留 `BI-AI科目映射表` 页签，用于展示和重建 BI 六级科目到费用大类、费用类别与预算发布口径的映射；该映射的源 Excel 候选、表头校验、空表容错、seed/reload 和 list read model 由 `apps/api/app/services/bi_ai_subject_mapping.py` 承载，路由只保留 HTTP 入口。旧 `BI科目维护` 不再作为可见维护页签。
- 当前已将 `BI科目匹配表.xlsx` 落库为 68 条 `bi_ai_subject_mapping` 配置；费用执行明细导入解析时允许按源 Excel 的“管控口径编码/名称”表头匹配 BI-AI 配置，并写入费用大类、费用类别和预算发布口径。该来源字段进入系统后统一命名为 `bi_ai_source_code` / `bi_ai_source_name`，当前页面预览和匹配结果导出显示为 BI-AI 源编码/名称，不作为旧 BI科目维护入口恢复。除费用执行明细本身由用户按期导入外，BI-AI、BI部门和费用预测规则等配置均应落库运行，不依赖临时 Excel 文件。
- **费用执行明细导入**支持 `本年实际导入`、`本年预算导入`、`上年实际导入` 三类导入；批次列表、覆盖写入、导出和删除按导入类别隔离，避免去年同期或预算导入与本年实际串数。
- **费用执行明细匹配结果**补齐费用大类、费用类别、预算发布口径、归口管理部门2和专项管控打标，用于前端展示、导出和后续费用执行口径追溯。
- **费用预算执行报表**本年实际只读取本年实际导入明细；去年同期优先读取 `上年实际导入`，若未导入则回退年度库上一年实际数据。月报格式的费用类型树和 1/2/3.1/3.2 区块按 `budget_release_caliber_mapped` 汇总，让 TeamSubmit 的预算发布口径模板在当前模块化 service 下生效。
- **费用类型树展示**对业务要求固定展示的 `超额奖金` 执行零值保留，不影响其他全零科目的默认隐藏规则。
- 本轮涉及新增表与新增字段，数据库影响确认表已归档为 `archive/handover/team_contributions/20260602/TeamSubmit_20260529_shier_expense_budget_db_impact.xlsx`；确认前不直接改现场数据库。

## 0.16 本轮需求变更（2026-05-14）— merge-release / Codex0508 全量回退包甄别合并

- 本轮从 `archive/releases/Codex_20260511_Codex0508全量回退包_含数据库_env_excel/` 中甄别合入与当前费用管理/多维分析边界一致的主要代码增量：**BI映射维护**、**费用预测逻辑配置**与**业务支出成本收入比分析**。
- 部门费用预算管理模块保留“BI映射维护”入口，当前只暴露 BI-AI科目映射表与 BI部门维护；旧管控口径到部门预算科目的 BI科目维护入口已退休。
- BI映射维护支持“归口管理部门 → 费用归属部门”别名映射，用于解决外部费用执行文件中的归口部门口径与系统部门科目口径不完全一致的问题。
- 预算数据输入新增“费用预测逻辑配置”入口，支持按“年度 + 版本 + 费用归属部门 + 部门预算科目”维护预测规则，规则类型包括手工/导入、余额分摊、指标表达式；当前 Excel 模板生成、表头别名、中文枚举归一、JSON 参数校验和导入 payload 构造由 `apps/api/app/services/expense_forecast_rule_import.py` 承载，规则保存/删除由 `apps/api/app/services/expense_forecast_rule_commands.py` 承载，规则列表/按 ID 定位由 `apps/api/app/services/expense_forecast_rule_read_model.py` 承载，路由只保留 HTTP 文件流、预览/应用编排和错误映射。
- 多维分析曾引入“业务支出成本收入比分析”入口，支持按主体、月份、事业群、产品和金额单位查看业务投入、业务产出与评估指标；当前“业务支出成本收入比分析”和“业务支出成本收入比维护”均统一归入部门费用预算管理模块。
- 本轮仍不合入来源包中的旧版导航、旧版 WorkArea、旧版数据库/env/dist 覆盖逻辑，避免回退当前数据科目维护、预算输出展示报表、智能报告/PPT 和统一 UI 规范。

## 0.17 本轮需求变更（2026-05-14）— Nick 模拟测算改造 v2

- 本轮从 `archive/releases/nick_20260514_模拟测算改造_v2/` 中仅甄别合入**模拟测算模块改造**，不合入来源包中与当前主线不一致的旧数据、旧导航、旧 WorkArea 或其它模块实现。
- “预算数据输入”下的模拟测算拆分为“模拟测算（正算）”与“模拟测算（倒算）”；正算页由自动刷新改为用户点击“开始计算”，并支持导出 Excel。
- 正算页取消旧“调整拨备，保持利润不变”测算列，结果表保留“26年基准情景”和“26年模拟测算（利润变动）”两列。
- 风险成本展示增强：“风险成本-基础拨备”下钻到贷款产品，展示产品风险成本、日均余额、风险成本率三层口径。
- 倒算页支持净利润目标与拨备覆盖率目标，用户可选择输出变量，系统按当前基准和模拟接口返回反推建议值与结果表。
- 模拟测算后端数据链路切换为当前数据科目体系：基准和结果读取 `data_account_metric_node`、`data_account_metric_binding`、`data_account` 与年度库 `budget_data`；模拟测算本身不再以 `report_account`、`report_data_mapping` 或旧 `driver_*` 表作为取数和联动依据。
- 后端提供 `/api/budget-simulation/export`，导出模拟参数和测算结果；旧 `/api/driver/*` 配置/测算路径已退休。

## 0.18 本轮需求变更（2026-05-14）— 测试环境服务器发布配置

- 本轮从 `archive/releases/关于测试服务器部署的内容/` 合入测试服务器发布配置，仅涉及部署脚本、端口、Host/CORS 与后端 uv 环境配置；不改变业务数据模型。
- 测试环境前端服务端口统一为 `8443`，Vite 监听 `0.0.0.0`，并放行 `guanheng.webank.com` Host；前端 `/api` 代理后端 `127.0.0.1:8009`。

## 0.19 本轮需求变更（2026-05-14）— 潘潘费用模块验收修正

- 部门架构维护导入必须支持业务 Excel 模版中的层级式稀疏行：主体行、事业群行、费用归属部门行可分行维护，子级行继承上方主体与事业群；导出工作表统一为 `数据模版`，便于回传导入。
- 费用预测逻辑配置应展示增强版两段式前端：先在“规则管理”中筛选、导入、版本复制和进入配置，再在“参数配置”中维护适用范围、生效控制、预测方案参数、表达式变量和备注。
- 本轮仅调整前端展示与导入解析逻辑，不覆盖数据库文件、env 或来源包旧运行环境。
- 测试环境后端服务端口统一为 `8009`；后端 CORS 放行 `127.0.0.1:8443`、`localhost:8443`、`guanheng.webank.com:8443`、`guanheng.webank.com` 和 `10.65.*.*:8443`。
- 根目录新增 `start.sh` / `stop.sh`，分别管理前端和后端进程，PID 写入 `var/pids/`，日志写入 `var/logs/`。
- 后端目录新增 `.python-version` 与 `pyproject.toml`，用于测试服务器通过 uv 创建隔离虚拟环境；`apps/api/app/config.py` 改为固定读取 `apps/api/.env`，避免不同启动目录导致环境变量丢失。
- 本轮暂不执行打包；后续打包发布时需包含部署脚本、后端环境配置、`apps/api/.env`、`var/data/`、模板和业务 Excel，并排除 `var/pids/`、`var/logs/`、依赖目录和历史 `archive/releases/`。

## 0.5 本轮需求变更（2026-04-28）

- 多维分析导航项“数据透视图”更名为“多年度数据透视图”。
- 左侧导航“智能分析报告”“智能演示PPT”采用灰色视觉态（功能建设中提示），但保留可点击打开能力。
- Agent 查询确认区的“已锁定查询维度”文案改为紧凑排版，减少空行。
- 透视建议说明从确认正文移除，统一由聊天侧“管衡推荐透视视角”卡片承载，避免重复。
- 多年度对比透视应用建议时，搜索框需保留 `pivot_search_text` 预填（不再被 compare 分支清空）。

## 0.6 本轮需求变更（2026-04-29）

- 新增“**全局自动刷新任务**”：后端每 **10 分钟**检查并执行一次“预算库公式重算 + 预算汇总刷新 + Compare 刷新”链路。
- 自动任务仅在“**至少 1 个有效登录会话**”存在时运行（会话以 `user_sessions.expire_time` 判定有效，过期会话先清理）。
- 每个年度库（`budget_{year}.db`）在 `settings` 表新增并维护键 `global_refresh_time_a`（简称 **A**，该年度库上次全局刷新时间）。
- `compare.db` 新增 `settings` 表并维护键 `global_refresh_time_b`（简称 **B**，Compare 上次全局刷新时间）。
- 自动任务流程（MUST）：
  1. 对每个年度库检查 `budget_data.need_calc=1`，若命中则执行公式重算；
  2. 重算后若该库任一 `budget_data.update_time > A`，则重建该库所有版本的 `budget_summary`，并重写 A；
  3. 若任一年度库 A 晚于 B，则刷新 `compare_budget_summary`，并重写 B。
- 底栏”数据库最后全局计算并刷新时间”统一显示 **B**；若 B 缺失则回退显示当前年度 `budget_summary` 最大 `update_time`。

## 0.7 本轮需求变更（2026-05-06）— ZLC

- 将产品科目从单层维护升级为**多层结构维护**，前端与后端统一支持 `parent_code + level` 的树形表达，产品导入模板与维护页均可维护父子层级。
- 数据科目与产品科目的绑定统一收敛为 **`data_account_metric_binding.scope_code`**：其值必须等于唯一指标号码的产品前缀，并直接来自 `product_type.product_code`。数据科目维护页只维护一套唯一指标号码。
- Excel 导入交互完成一轮改造：通用导入弹窗支持”新增/更新（upsert）”与”覆盖（replace）”两种模式，预算基础数据导入结果文件明确区分新增、覆盖与失败单元格，提升批量维护可控性与回溯性。
- `data_account` 不再保存第二套产品身份字段；产品编码与产品中文名统一通过绑定表和产品科目表读取，详见 Database PDD §0.2。
- 产品科目编码校验更新为产品组织树规则：`CORP` 为全行/公司级保留范围，机构/群组使用单个大写字母（如 `A`），一级产品使用 `A01` 形态，二级产品使用 `A0101` 形态。
- 同步修复了导入反馈与前端选择交互的若干问题，重点改善产品编码回显、导入结果提示与用户操作闭环。

> 本次由 ZLC 提交，主合并人完成代码合并与 PDD 更新。待解决问题详见 CHANGELOG。

## 0.8 本轮需求变更（2026-05-08）— merge-release

- 合入 Panpan 提交的**费用管理模块群**，在现有预算系统中新增部门预算科目维护、费用执行明细导入、费用预测表、费用预算执行报表、数据同步管理五个入口。
- **部门预算科目维护**支持五级树状目录、科目公式文本和 Excel 导出，作为费用预测与费用执行报表的科目口径。
- **费用执行明细导入**支持读取费用执行源文件，按费用归属部门与预算科目进行匹配校验，提供预览与批量写入链路。
- **费用预测表**支持主体、事业群、费用归属部门三种视角，按月维护预测值，并与已导入的实际执行月份组合展示。
- **费用预算执行报表**支持查询模式与模板模式，展示月度实际、累计实际、年度预算、执行率、预算进度和同比指标。
- **数据同步管理**历史上曾作为费用数据入口总控台；当前只保留初始化框架同步，部门费用执行实际统一通过“费用执行明细导入”进入费用闭环。
- 本次合并历史上曾保留原“预算基础数据维护”和独立预算预测驱动入口；当前独立预算预测驱动入口已退休，预算/实际维护统一进入预算输入，情景测算进入模拟测算。

> 本轮合并仅纳入费用模块相关增量，未采用来源包中会回退现有数据科目范围、产品层级与预算预测驱动逻辑的旧实现。

## 0.9 本轮需求变更（2026-05-09）— merge-release

- ZLC 提交的“工作台和参数模板”增量已退休：旧“预测预算工作台”和“预算基本假设 / 参数与模板维护”隐藏页面、后端接口和私有表均已删除。
- 预算配置继续收敛到当前导航中的 **数据科目维护表**、费用预测逻辑配置和模拟测算等专业 Module。
- 不再提供 `/api/forecast-workbench/*` 或 `/api/budget-assumptions/*` Interface。
- 本轮采用增量合并方式保留当前主干中的费用模块、智能报告、数据科目指标树和模拟测算等既有能力，未用来源包旧版本覆盖当前主线。

## 0.10 本轮需求变更（2026-05-09）— Codex

- 固化数据科目编码分工：所有数据科目统一使用产品前缀指标主键作为唯一指标号码，例如叶子 `A05.01.01.001`，父节点派生汇总直接使用父节点产品前缀指标主键。系统不再接受 `A####` / `C####` / `K####` 或旧后缀拼接码作为数据科目编码。
- 数据科目维护页当前采用“产品前缀指标树 + 数据科目明细”的维护方式；新增人工数据科目时用户选择产品前缀指标主键，系统自动生成同码唯一指标号码；父节点数据科目由 `metric_rollup_method` 自动生成。
- 预算输入和模拟测算需要的可维护预算行统一读取数据科目指标树绑定作为分类来源；能否手工维护预算值由 `allow_manual_entry` 控制，公式结果仍由重算链路按产品维度展示。
- 旧报告科目表已删除；报告侧业务层级直接来自 `data_account_metric_binding` 与 `data_account_metric_node`。

## 0.11 本轮需求变更（2026-05-11）— merge-release

- 合入 Kevin 提交的“模拟测算”模块，在“预算数据输入”下新增“模拟测算”入口，支持按贷款产品选择管理贷款时点规模、贷款收益率、联合贷款收益率、风险成本率等测算参数。
- 前端新增情景参数表和测算结果表：基准情景直接引用年度预算库中的当前预算/事实结果，模拟情景支持手工录入，并展示盈利性指标、风险指标以及“调整拨备、保持利润不变”口径。
- 后端保留模拟测算基准值、模拟结果和导出接口；旧“预算驱动因素页面”已下线，当前主线不再使用 `driver_*` 表，测算基准值直接读取标准指标树、数据科目绑定和年度预算库数据。
- 本轮合并采用增量方式，仅纳入模拟测算相关页面、DTO 和路由接口；未覆盖当前主线的工作台、参数模板、智能报告、数据科目指标树等已有改动。

## 0.12 本轮需求变更（2026-05-11）— merge-release

- 合入潘潘 0511 费用模块修复与导出增强，覆盖部门预算科目维护、部门科目维护、费用执行明细导入、费用预测表和费用预算执行报表五个费用管理闭环模块。
- 部门预算科目维护新增“归口管理部门”字段，支持搜索、编辑和 Excel 导出；部门科目维护新增主体维度，并在部门名称变更时同步费用框架、费用预测和费用执行相关引用。
- 费用预测表新增“全年预测-年度预算”“预算执行率”“业务报送”“资划建议”“资划建议-业务报送”等经营分析列，支持金额单位切换、字段选择导出、按事业群导出，以及系统导出模板再导入。
- 费用预算执行报表补齐月报格式导出能力，支持部门模式、科目模式与多工作表月报导出，树形汇总行在 Excel 中保留公式，导出口径跟随页面当前金额单位。
- 费用执行明细导入改为优先读取系统主数据中的部门科目和部门预算科目口径，减少对外部框架文件的运行时依赖。

## 0.13 本轮需求变更（2026-05-11）— merge-release / Codex0508 规范包

- 合入 `Codex_20260511_Codex0508项目规范打包` 中的费用数据同步规范补丁，明确数据同步管理页由“部署机固定文件读取”调整为“前端上传 Excel 后由后端解析入库”的联调方式。
- 数据同步管理页将费用框架入口收敛为“初始化框架导入”，用于历史初始化或批量导入；日常部门、科目和费用配置维护以系统主数据维护页面为准。
- 初始化框架导入支持 `.xls`、`.xlsx`、`.xlsm` 文件选择，未选择文件时禁用预览或同步按钮，并在操作前完成上传文件校验。费用执行实际通过“费用执行明细导入”页面进入当前明细表，不再通过数据同步管理写旧月度快照。
- 后端不得再从 `resources/business_inputs/` 或部署机桌面目录自动读取费用框架/费用执行源文件；导入必须由用户显式上传，费用执行实际运行时读取当前费用执行明细导入表。
- 后端既有上传接口 `/api/expense-budget-execution/admin/framework-preview`、`/framework-sync` 作为初始化框架同步链路入口；预览/同步命令由 `expense_budget_execution_framework_sync.py` 编排，路由只负责上传文件解析和 HTTP 响应。状态接口只展示框架同步元数据和当前表行数，不再返回旧 `actual_import` 同步状态。旧 `/actual-sync` 月度快照入口已退休，费用执行实际以 `/api/expense-actual-import/*` 明细导入为准。
- 本轮不涉及 Agent 能力变化。

## 0.14 本轮需求变更（2026-05-12）— 预算输出展示报表一期

- 预算管理模块明确为“预算工作台和规则配置 → 预算输入 → 预算输出”闭环，左侧导航新增顶层分组“预算输出”。
- 预算输出一期新增“预算展示报表”入口，页面包含“全行总表”“分产品概览”“单产品明细”三个页签；历史样式基线 `预算分析报表.xlsx` 已归档到 `archive/handover/legacy_import_workbooks/business_inputs_legacy_20260602/`，当前运行展示以 `budget_output_display_item` 与前端实现为准。
- 预算展示报表读取系统真实预算数据，展示行项目来自 `budget_output_display_item`，不硬编码或运行时读取 Excel 底稿行。
- 预算展示配置支持从当前数据库导出标准导入模板，并通过显式上传覆盖导入；历史 `科目和层级表.xlsx` 只能作为人工整理材料，不是运行时初始化来源。
- 报表顶部采用年份、预算基准版本、预测版本多选、产品筛选和单位的紧凑取数栏；默认展示选定年份的年初预算基准与最新预测版本。
- 分产品概览和单产品明细支持选择产品科目树中的父级或叶子节点；父级产品节点按其全部下级产品汇总，默认全行总表可见。
- 报表表格采用 Excel 式交互：展示配置行默认展开到2级，行展开/收起在表格行首完成；月度明细列在版本表头中展开/收起。
- 后端只读接口 `/api/budget-output/display-report` 按展示配置中绑定的唯一指标号码精确读取年度库 `budget_data`，父子展示层级只负责展示结构，不承载计算或自动匹配旧 Excel 编码。

## 0.15 本轮需求变更（2026-05-13）— merge-release / Codex0508 完整回退包

- 合并 `archive/releases/Codex_20260511_Codex0508完整回退包_含数据库与env/` 的交付记录与 PDD 补丁，将其登记为内部受控回退资产，而不是当前运行主线的功能来源。
- 完整回退包用于误操作恢复、数据库/环境配置定点回放和历史状态留档；恢复前必须先备份当前项目目录，恢复动作应由 PMO/CTO 确认后执行。
- 本轮不将回退包中的旧源码、旧 `var/data/*.db`、根目录数据库、`apps/api/.env` 或 `dist/` 覆盖当前项目，避免回退 2026-05-12 之后已形成的预算输出、数据科目维护、智能报告/PPT 与 UI 规范成果。
- 回退包中发现的 `business_cost_income_ratio` 相关页面和接口在初次登记时未进入运行主线；2026-05-14 经用户确认“成本收入比模块也需要整合”后，已作为后续增量纳入 `0.16` 合并范围。
- 本轮不改变 Agent 能力、不新增运行导航入口、不改变当前权限模型。
