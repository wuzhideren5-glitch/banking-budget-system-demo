# PDD 章节补丁 — Codex — 2026-05-11（完整回退包）

> 请主合并人使用 /merge-release 将此补丁内容合入正式 PDD。

---

## System PDD 建议新增 (§0.10)

- 为 Codex 2026-05-08 项目补充一份**完整回退包**，用于内部留档、问题回滚与误操作恢复，不替代对外的规范交付包。
- 完整回退包保留项目当前运行所依赖的关键状态，包括数据库文件、环境变量文件、配置表所在库、前端构建产物以及项目内的辅助数据文件，确保后续需要时可按当前状态整体恢复。
- 该回退包的使用场景为“代码或数据被错误修改后回退到本次快照”，恢复时以整目录覆盖或按数据库/配置文件定点回放为主，不要求接收方重新构造配置表与运行环境。

> 插入位置：System PDD 的 `## 0.9` 之后，新增 `## 0.10` 小节。

---

## Database PDD 建议新增 (§0.5)

- 本次无数据库结构变更，无需更新表结构定义。
- 本次交付新增的是**数据库级回退快照策略**：在内部回退包中保留 `var/data/`、项目根目录、`apps/api/` 目录下的 SQLite 数据库文件，以便后续按文件快照恢复配置表、版本数据与对比分析数据。
- 该策略不改变任何表、字段或约束，仅补充交付与运维层面的数据保全方式。

---

## Agent PDD 建议新增

- 本次无 Agent 变更，无需更新。

---

## 补充说明

1. **回退目的**：本压缩包用于内部回滚，不建议直接外发，因为其中包含真实数据库与环境配置。
2. **保留内容**：除源码外，额外保留 `apps/api/.env`、`var/data/*.db`、根目录与 `apps/api/` 下的数据库文件、`dist/` 构建结果及其他运行期辅助文件。
3. **恢复方式**：如后续误操作，可先备份当前目录，再使用该回退包解压覆盖项目目录，或只定向恢复数据库文件与 `.env`。
4. **安全提示**：由于包含真实配置与数据，压缩包应仅保存在受控位置，不应发给无权限人员。
